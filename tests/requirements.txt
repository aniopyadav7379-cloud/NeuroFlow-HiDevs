# Test-only dependencies (Task 42) — not needed by the running
# application, only by pytest/locust/benchmark scripts in tests/.
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
locust==2.31.8
reportlab==4.2.5
